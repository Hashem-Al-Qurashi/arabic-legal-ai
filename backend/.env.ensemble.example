# ===========================================
# ENSEMBLE SYSTEM ENVIRONMENT CONFIGURATION
# ===========================================

# Core application settings (existing)
SECRET_KEY=your-secure-secret-key-here
DATABASE_URL=sqlite:///./arabic_legal.db
ENVIRONMENT=development

# ===========================================
# ENSEMBLE AI MODEL API KEYS
# ===========================================

# Generator Models (4 required for full ensemble)
OPENAI_API_KEY=sk-your-openai-key-here          # ChatGPT (Generator + Judge)
DEEPSEEK_API_KEY=sk-your-deepseek-key-here      # DeepSeek (Generator)
GROK_API_KEY=xai-your-grok-key-here             # Grok (Generator) 
GEMINI_API_KEY=your-gemini-key-here             # Gemini (Generator + Judge)

# Judge Models (3 required for consensus)
ANTHROPIC_API_KEY=sk-ant-your-claude-key-here   # Claude Sonnet (Judge)
# Note: OpenAI and Gemini keys above also used as judges

# ===========================================
# ENSEMBLE SYSTEM CONFIGURATION
# ===========================================

# Processing settings
ENSEMBLE_TEMPERATURE=0.7                         # Temperature for all models
ENSEMBLE_MAX_TOKENS=2000                        # Max tokens per model
ENSEMBLE_CONTEXT_CHUNKS=20                      # Number of legal chunks to retrieve

# Cost control
ENSEMBLE_MAX_COST_PER_QUERY=0.50                # Safety limit ($0.50)
ENSEMBLE_TRACK_COSTS=true                       # Track API costs

# Data collection
ENSEMBLE_SAVE_TRAINING_DATA=true                # Save data for fine-tuning  
ENSEMBLE_TRAINING_DATA_PATH=data/ensemble_training_data.jsonl

# ===========================================
# API ENDPOINT CONFIGURATION
# ===========================================

# Model-specific base URLs (if different from defaults)
DEEPSEEK_BASE_URL=https://api.deepseek.com/v1
GROK_BASE_URL=https://api.x.ai/v1
GEMINI_BASE_URL=https://generativelanguage.googleapis.com/v1beta/openai
CLAUDE_BASE_URL=https://api.anthropic.com/v1

# ===========================================
# TESTING & DEVELOPMENT
# ===========================================

# Enable ensemble endpoint in development
ENABLE_ENSEMBLE_ENDPOINT=true

# Test mode (uses mock responses, no API calls)
ENSEMBLE_TEST_MODE=false

# Debug logging
ENSEMBLE_DEBUG_LOGGING=true

# ===========================================
# PRODUCTION WARNINGS
# ===========================================

# ⚠️  ENSEMBLE SYSTEM COSTS:
# - ~$0.29 per query with all 4 generators + 3 judges
# - 1,000 queries = ~$290
# - 10,000 queries = ~$2,900
# 
# 💡 RECOMMENDATIONS:
# - Start with 2-3 models to reduce costs
# - Use ensemble for investor demos only
# - Switch to fine-tuned single model after data collection
# - Monitor costs carefully in production

# ===========================================
# MINIMUM CONFIGURATION FOR TESTING
# ===========================================

# To test ensemble with minimal cost:
# 1. Set OPENAI_API_KEY (required - generator + judge)
# 2. Set DEEPSEEK_API_KEY (cheap generator)
# 3. Ensemble will work with 2 models + 1 judge
# 4. Cost per query: ~$0.08 instead of $0.29

# ===========================================
# API KEY SOURCES
# ===========================================

# OpenAI: https://platform.openai.com/api-keys
# DeepSeek: https://platform.deepseek.com/
# Grok (X.AI): https://console.x.ai/
# Gemini: https://aistudio.google.com/app/apikey  
# Claude: https://console.anthropic.com/